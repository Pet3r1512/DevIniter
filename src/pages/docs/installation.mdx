import InstallBlock from "../../components/Documents/InstallBlock"
import QuestionBlock from "../../components/Documents/QuestionsBlock"
import TipsBlock from "../../components/Documents/TipsBlock.tsx"
import ScriptBlock from "../../components/Documents/ScriptBlock.tsx"
import HighlightedText from "../../components/Documents/HighlightedText.tsx"

# Installation

To get started with DevIniter templates, please follow these steps:

## Prerequisites

Ensure you have the following tools installed on your system:

- <HighlightedText content={{href: "https://nodejs.org/", text: "Nodejs", target: "_blank" }} /> (v20.0.0 or higher)
- <HighlightedText content={{href: "https://pnpm.io/", text: "pnpm", target: "_blank" }} /> (v9.0.0 or higher)
- <HighlightedText content={{href: "https://www.npmjs.com/", text: "npm", target: "_blank" }} /> (v10.0.0 or higher)

<strong>Note:</strong> We are actively working to expand support for additional Node.js versions and package managers such as Yarn.

## Scaffolding Template

We recommend starting a new template using `deviniter`, which downloads template for you automatically. To install a template, run:

<ScriptBlock script={
    {command: "npx",
    content: "@deviniter/cli@latest"}
}/>

After running this command, our CLI will guide you through the setup process:

<QuestionBlock questions={[
    {
        question: "What is your project name?",
        description: "Enter a name for your new project (lowercase, no spaces)"
    },
    {
        question: "Select a template: (use arrow keys to navigate)",
        options: [
            "Next.js",
            "Vite",
        ]
    },
]} />

If you’re new to DevIniter, refer to our <HighlightedText content={{href: "/docs/template_structure", text: "Template Structure"}} /> guide for a comprehensive overview and to enhance your experience with our templates.

### Next Steps

After successful installation:
1. Review our guide <HighlightedText content={{href: "/docs/getting-started", text: "Getting Started"}} />
2. Explore the <HighlightedText content={{href: "/docs/template_structure", text: "Template Structure"}} />
3. Check out our <HighlightedText content={{href: "", text: "Features Overview"}} /> (Coming Soon)


## Installing Dependencies

Now, you need to navigate to your project:

<ScriptBlock script={
    {command: "cd",
    content: "project-name"}
}/>

After successfully scaffolded your favorite template, you need to install all dependencies by running:

<InstallBlock command={[
    {
      value: "pnpm",
      script: "pnpm install",
    },
    {
      value: "npm",
      script: "npm install",
    },
  ]} />

<TipsBlock tips={[
    <p>- Expected setup time is around 2-5 minutes for first-time users. </p>,
    <p>- Make sure to star our repository on GitHub to stay updated with new features and templates. </p>
]} />

## Starting Your Project

Then, start the project locally with one of the following commands:

<InstallBlock command={
    [
        {
            value: "pnpm",
            script: "pnpm dev"
        },
        {
            value: "npm",
            script: "npm run dev"
        },
    ]
}/>

If there is no other application running on port 3000, your project will start at <HighlightedText content={{href: "http://localhost:3000", text: "localhost:3000", target: "_blank"}} />.

<TipsBlock tips={[
    <p>- With <span className="text-[#e4c1f9] font-semibold">Vite</span> template, your project will start at <HighlightedText content={{href: "http://localhost:5173", text: "localhost:5173", target: "_blank"}} /> in default</p>
]} />

## Troubleshooting

### Common Issues

1. **Port Already in Use**
   ```bash
   Error: Something is already running on port 3000
   ```
   **Solution**: Either close the application using port 3000 or modify the port in your configuration file.

2. **Node Version Mismatch**
   ```bash
   Error: The engine "node" is incompatible with this module
   ```
   **Solution**: Use nvm (Node Version Manager) to install and switch to the required Node.js version:
   <ScriptBlock script={
       {command: "nvm",
       content: "use 20"}
   }/>

3. **Package Installation Fails**
   - Clear your package manager cache:
     <InstallBlock command={[
         {
           value: "pnpm",
           script: "pnpm store prune",
         },
         {
           value: "npm",
           script: "npm cache clean --force",
         },
     ]} />
   - Ensure you have the necessary permissions
   - Check your network connection

### Still Having Issues?

- Check our <HighlightedText content={{href: "/docs/faq", text: "FAQ"}} /> page (Coming Soon)
- Join our <HighlightedText content={{href: "https://discord.gg/deviniter", text: "Discord community"}} /> (Coming Soon)
- Open an issue on our <HighlightedText content={{href: "https://github.com/Pet3r1512/DevIniter/issues/new?labels=bug&template=bug-report---.md", text: "GitHub repository"}} />

### Next Steps

After successful installation:
1. Review our <HighlightedText content={{href: "/docs/getting-started", text: "Getting Started"}} /> guide
2. Explore the <HighlightedText content={{href: "/docs/template_structure", text: "Template Structure"}} />
3. Check out our <HighlightedText content={{href: "", text: "Features Overview"}} /> (Coming Soon)